---
name: project-reservas-saas
description: SaaS multi-tenant de reservas online (Supabase + React) — proyecto en curso
metadata:
  type: project
---

SaaS multi-tenant de reservas para negocios locales. Dos tipos de tenant: `citas` (clínicas/peluquerías) y `restaurante` (aforo/comensales). Sin registro público: el super-admin crea los negocios.

**Infra**: proyecto Supabase en vivo `reservas-saas` (project_id `fjpbruwczuovvynhlnzv`, org `zreqgntqklgqzjwtsyto`, región eu-west-1). URL `https://fjpbruwczuovvynhlnzv.supabase.co`. Publishable key `sb_publishable_CKlKGvIyTSN3iUh-XkNlUw_qhuYabRs`.

**Credenciales demo**: super-admin `admin@reservas.test` / `Admin1234!`; staff barbería `staff@barberia.test` / `Barberia1234!`. Negocio demo tipo citas: slug `barberia-demo`.

**Decisiones**: React+Vite+TS+Tailwind v3, Resend para email, Edge Functions Deno. Motor de disponibilidad vive en Postgres (`get_available_slots`) como fuente única de verdad. Widget/paneles nunca leen tablas directamente para lo público: usan RPCs SECURITY DEFINER. Reservas se crean por Edge Function (service_role), no por anon.

**Fase 1 (COMPLETADA y verificada en navegador)**: cimientos + tipo `citas` end-to-end. Migraciones 0001-0005 aplicadas. Edge Functions desplegadas: create-booking, send-confirmation-email, whatsapp-reminders, admin-create-business. Monorepo npm workspaces: apps/widget (5174), apps/dashboard (5173), packages/shared. Ambas apps compilan limpias (`npm run build`). Widget, panel de negocio (8 páginas) y super-admin funcionan.

**Tenant demo restaurante** creado vía super-admin: slug `restaurante-la-plaza`, staff `staff@restaurante.test` / `Restaurante1234!`.

**Fase 2 (COMPLETADA y verificada)**: restaurante end-to-end. Migración 0006 (dining_shifts.booking_duration_min + `get_available_dining_slots` + `create_public_dining_booking`). Widget con flujo comensales→día→hora (agrupada por franja). `create-booking` gestiona ambos tipos (ramifica por `dining_shift_id`). Panel adapta nav por tipo: página Franjas (`business/Franjas.tsx`), NuevaReserva ramifica, Agenda/Dashboard muestran "Mesa · N comensales". Ambas apps compilan limpias.

**Agenda semanal (COMPLETADA)**: `business/Agenda.tsx` con toggle Día/Semana, rejilla de calendario (WeekGrid) con eje horario, bloques por estado y modal de edición al clic. OJO: `ymdInTz`/`weekdayInTz` esperan un `Date`, no un string ISO — envolver siempre en `new Date()` (un string a `Intl.formatToParts` da "Invalid time value").

**Integraciones POR NEGOCIO (COMPLETADA)**: migración 0007 `business_integrations` (email_from, resend_api_key, whatsapp_phone_number_id, whatsapp_token). Secretos protegidos: RLS solo super-admin; staff usa RPCs `set_business_integration`/`get_business_integration` (esta última devuelve estado enmascarado con has_resend_key/has_whatsapp_token, sin secretos). Edge Functions (create-booking, send-confirmation-email, whatsapp-reminders) leen credenciales del negocio con service_role; env globales como fallback. Panel Configuración → sección "Integraciones". Verificado: lectura directa del secreto por el navegador bloqueada por RLS. NOTA: la barbería demo tiene una resend key FALSA de prueba (re_demo_testkey_123456) — email fallará hasta poner una real.

**Super-admin ampliado (COMPLETADO)**: `admin/BusinessDetail.tsx` (ruta `/admin/negocio/:id`) con 3 pestañas: Dashboard por negocio (KPIs+gráfico), Editar negocio (name/slug/color/tz/is_active/aforo; tipo bloqueado), Integraciones (usa `components/IntegrationsForm.tsx`, compartido con el panel de negocio). Listado de negocios con KPIs de plataforma. El super-admin puede gestionar todo porque `is_business_member` devuelve true para super_admin.

**Sistema de mesas físicas tipo TheFork Manager — Fase 1 (COMPLETADA y verificada en navegador, 2026-09-17)**: siguiendo el documento de referencia del usuario (5 capas: calendario→stock→mesas→duración→reglas), se implementó la capa 3 (mesas físicas) y parte de la capa 4 (duración por nº de comensales) para el tipo restaurante, manteniendo el aforo agregado (capa 2) como límite adicional. Migración 0009 (`dining_tables.sql`): tablas `dining_zones`, `dining_tables`, `dining_duration_rules`; columna `bookings.dining_table_id`; constraint `EXCLUDE` (btree_gist) que impide solapes físicos por mesa a nivel de Postgres. `get_available_dining_slots` y `create_public_dining_booking` ahora exigen mesa libre con asignación best-fit automática (menor desperdicio→prioridad→nombre) + override manual vía `p_table_id`. Si un negocio no tiene mesas cargadas, el motor sigue funcionando solo con aforo agregado (retrocompatible). Nueva RPC `get_dining_table_options` para elegir/reasignar mesa desde el panel. Panel: página nueva `business/Mesas.tsx` (zonas+mesas), `Franjas.tsx` con editor de reglas de duración por rango de pax, `NuevaReserva.tsx` con selector de mesa (automático/manual), `Agenda.tsx` con reasignación de mesa en el modal de reserva. Datos demo: 2 zonas y 10 mesas para `restaurante-la-plaza`.

OJO — bug real encontrado y corregido durante esta fase: al añadir `p_table_id` a `create_public_dining_booking` se creó sin querer una SEGUNDA sobrecarga de la función (10 vs 11 argumentos) en lugar de reemplazarla, y PostgREST no podía desambiguar cuando el cliente omite `p_table_id` (caso automático, ya que `JSON.stringify` elimina claves `undefined`). Solución: **nunca añadir un parámetro nuevo a una función RPC ya expuesta sin `drop function` de la signature vieja primero**, o el `create or replace` con distinta firma crea una sobrecarga fantasma. Ver migración `0009c_drop_old_dining_booking_overload`. [[feedback-supabase-rpc-overloads]]

También se descubrió que el negocio demo `restaurante-la-plaza` tenía `is_active=false` (previo a esta sesión, causa desconocida) — se activó manualmente para poder probar; conviene que el usuario confirme si fue intencionado.

**Restaurante 100% configurable por negocio (COMPLETADA y verificada, 2026-09-17)**: a petición explícita del usuario, todo el motor de restaurante se hizo ajustable por negocio y editable en cualquier momento tras la creación. Migraciones 0010 (enum `booking_status` + valor `pendiente`, en migración propia porque un valor de enum no se puede usar en la misma transacción en que se crea) y 0011 (`dining_settings.sql`):
- `dining_settings` (1:1 por negocio): `min_lead_minutes`, `max_advance_days`, `min_party_online`/`max_party_online`, `require_manual_confirmation`. RPC pública `get_public_dining_settings` para que el widget filtre el selector de comensales.
- `dining_shifts` +columnas: `last_call_time`, `pacing_enabled` + `max_covers_per_slot`/`max_bookings_per_slot` (activable — si no, solo cuenta el aforo total), `online_max_covers` (stock reservado para teléfono/walk-in), `allow_double_turn`, `cleanup_min`.
- `dining_table_combos` (combinaciones de mesas manuales, con `table_ids uuid[]`) — Fase 3 del roadmap original adelantada a petición del usuario.
- `dining_table_busy()`: helper único que centraliza la lógica de "mesa ocupada" (doblar-mesa + limpieza + pertenencia a combo), usado por `get_available_dining_slots`, `create_public_dining_booking` y `get_dining_table_options`.
- **Todas** las reglas nuevas (antelación, mín/máx comensales online, stock por slot/online) solo aplican a `p_channel='web'`; las reservas manuales del staff siempre ven el aforo completo sin restricciones — filosofía "el jefe de sala sabe más que el algoritmo" ya aplicada a la asignación de mesas.
- "Excepciones" (cierres puntuales) no necesitaron cambios: ya eran la tabla `blocks` con scope='business', editable por el negocio en Bloqueos.
- Panel: `Franjas.tsx` (todos los campos nuevos por franja), `Mesas.tsx` (sección Combinaciones), `Configuracion.tsx` (sección "Reglas de reserva"), `NuevaReserva.tsx` (pasa `p_channel:'manual'`), `Agenda.tsx` (badge/estilo `pendiente`, muestra nombre de combo, oculta reasignación manual de mesa para reservas con combo).
- Widget: `get_public_dining_settings` filtra comensales visibles; mensaje "⏳ Solicitud recibida" vs "✅ ¡Mesa reservada!" según `status` devuelto por `create-booking` (Edge Function redeployada). Email de confirmación también distingue pendiente/confirmada (`isPending` en `buildConfirmationEmail`).
- **Otro bug de sobrecarga de RPC evitado** (ver [[feedback-supabase-rpc-overloads]]): `get_available_dining_slots` y `get_dining_table_options` cambiaron de firma (nuevo parámetro `p_channel` / `p_shift_id`) — se hizo `drop function` explícito de las firmas viejas en la misma migración antes de crear las nuevas.
- Verificado en navegador y con SQL directo: pacing on/off, doblar-mesa+limpieza, combinación de mesas (best-fit cae a combo cuando ninguna mesa individual encaja), confirmación manual → estado `pendiente` visible en widget y Agenda.

**Fase 2 — Plano de sala en vivo (COMPLETADA y verificada, 2026-09-17)**: migraciones 0012 (enum `sentada`), 0013 (enum `walkin`), 0014 (`dining_assign_table`, `create_walkin_booking`, notas/tags de cliente, Realtime). Página nueva `business/PlanoSala.tsx`: cuadrícula de mesas en vivo por zona (estado derivado en cliente a partir de `now` + reservas del día: libre/reservada_pronto/reservada_ahora/retrasada/sentada/a_punto_terminar), suscrita a Supabase Realtime (`useBookingsRealtime` en hooks.ts, canal `postgres_changes` sobre `bookings` filtrado por `business_id`). Walk-ins: botón "+ Walk-in" en mesa libre → `create_walkin_booking` (reutiliza el motor de asignación `dining_assign_table`, extraído de `create_public_dining_booking` para no duplicar lógica). Ficha de cliente (`Clientes.tsx`) con etiquetas fijas (VIP/Habitual/Alérgico/Prensa/Problemático) y notas privadas editables. WhatsApp 24h e informes básicos YA estaban hechos desde antes de Fase 1 (no eran pendientes reales).

OJO — al probar `(create_walkin_booking(...)).* ` en SQL directo, Postgres invoca la función UNA VEZ POR COLUMNA del tipo de retorno (aquí ~20, las de `bookings`), así que una función con INSERTs se ejecuta múltiples veces y cualquier chequeo de aforo acumulativo salta con datos fantasma. Para probar funciones que devuelven una fila de una tabla con efectos secundarios, usar siempre `select * from fn(...)` (sintaxis de función-tabla), nunca `select (fn(...)).*`.

**Fase 3 sin Stripe (COMPLETADA y verificada, 2026-09-17)**: usuario confirmó no tener aún cuenta de Stripe → se dejó huella bancaria/prepago para cuando la tenga (aunque sea de test), y se avanzó con el resto. Migraciones 0015 (`waitlist`, `review_requests_log`, `businesses.google_review_url`/`waitlist_template_name`) y 0016 (`import_customer`).
- **Lista de espera**: tabla `waitlist` + sección en `PlanoSala.tsx`. Botón "Avisar" llama a la Edge Function `notify-waitlist` (verify_jwt=true; usa un cliente "como el usuario" con el Authorization forwarded para que RLS valide pertenencia al negocio antes de tocar secretos vía service_role) que envía una plantilla de WhatsApp (helper compartido nuevo `_shared/whatsapp.ts`, extraído de `whatsapp-reminders` para no duplicar). Botón "Sentar" reutiliza `create_walkin_booking`.
- **CSV de clientes**: parser CSV propio en `Clientes.tsx` (con soporte de comillas/comas). OJO — bug real encontrado: `supabase.from('customers').upsert(payload, {onConflict:'business_id,phone_norm'})` falla con `42P10 there is no unique or exclusion constraint matching the ON CONFLICT specification` porque el índice único de `customers` es PARCIAL (`where phone_norm is not null`) y PostgREST no puede expresar esa condición en `on_conflict` (solo acepta lista de columnas). Solución: RPC `import_customer` que hace el mismo `insert ... on conflict (...) where phone_norm is not null do update` que ya usan los RPC de creación de reserva. **Lección: cualquier upsert desde el cliente (PostgREST) sobre una tabla con índice único parcial debe pasar por un RPC, nunca por `.upsert()` directo.**
- **Petición de reseña**: cron `request-reviews` (mismo patrón sent+log que `whatsapp-reminders`), envía solo si `businesses.google_review_url` está configurado. Probado con `curl` directo a la Edge Function (idempotencia confirmada: segunda llamada = 0 enviados, 1 omitido).
- Al probar `notify-waitlist` desde el navegador, el error mostrado al usuario era el genérico "Edge Function returned a non-2xx status code" en vez del mensaje real — `supabase.functions.invoke` no expone el cuerpo JSON del error directamente, hay que extraerlo de `error.context.json()` (mismo patrón que ya usaba `createBooking` en el widget). Se corrigió en `PlanoSala.tsx`.

**Fase 4 revisada con el usuario (2026-09-17), sin cambios de código**: de la lista original ("mesas combinables automáticas · límites por origen · resumen de reseñas con IA · multi-local · API/webhooks"), las dos primeras ya estaban hechas (Fases 1 y 3). Se preguntó por las otras tres: reseñas con IA → pendiente (usuario no tiene acceso a la API de Google Business Profile, igual que Stripe); multi-local → el selector de negocio ya existente (`Layout.tsx`, `businesses.length > 1`) es suficiente, el usuario declinó un dashboard agregado; API/webhooks → el usuario dijo que no es prioritario ahora. No se tocó código en esta fase, solo se documentó el estado.

**Fase 5 — Profesionales N:N + color, selector en widget, borrado de negocios, horarios rápidos, antelación máxima, clientes manuales y Google Calendar (COMPLETADA y verificada en Cloudflare, 2026-09-18)**: sesión larga a petición explícita del usuario ("todas las cosas que te he pedido"), verificada contra producción real en Cloudflare (no localhost) por instrucción directa del usuario — ver [[project-turnigo-deploy]] para las URLs y el flujo de deploy (push a `main` → Cloudflare Workers Builds solo).
- **`services`↔`professionals` pasa de 1-a-1 a N:N** (migraciones 0017-0019): tabla puente `service_professionals`, `professionals.color` con paleta automática. `get_available_slots`/`create_public_booking` ganan `p_professional_id` (null = "cualquiera disponible", resuelto vía `lateral` fan-out sobre los profesionales asignados). Widget: paso nuevo de selección de profesional solo si el servicio tiene >1 asignado.
- **OJO — bug real que yo mismo introduje y corregí**: al reescribir `create_public_booking` en la migración 0018 partí del código leído en `0003` durante la exploración inicial (versión vieja), no de la definición ACTUAL en la BD, y reintroduje sin querer un bug ya corregido en `0008` (upsert de cliente contra el índice inexistente `(business_id, phone)` en vez de `(business_id, phone_norm)`). Rompía TODA reserva con teléfono. Solo lo detecté al completar una reserva real end-to-end en el widget desplegado — ni compilar ni probar la rama nueva por SQL directo lo habría pillado. Corregido en `0019`. **Lección para el futuro: antes de reescribir cualquier función ya expuesta, sacar su definición ACTUAL con `pg_get_functiondef` (o la migración de número más alto que la toque), nunca fiarse de lo leído en la exploración temprana.**
- **Borrado de negocios (super-admin)**: `DeleteBusinessModal` (Businesses.tsx + BusinessDetail.tsx) con confirmación escrita (nombre exacto). Usa `DELETE` directo — la RLS `businesses_delete` y los `on delete cascade` ya existentes bastan, no hizo falta Edge Function.
- **`WindowsEditor` extraído a componente compartido** (`apps/dashboard/src/components/WindowsEditor.tsx`, antes duplicado en Servicios.tsx y Configuracion.tsx): botón "copiar franja a todos los días" + panel "rellenar rápido" (días activos + franja común).
- **`businesses.max_advance_days`** (migración 0020): antelación máxima de reserva online para citas, mismo patrón que `dining_settings.max_advance_days`. Nuevo `p_channel` en `get_available_slots`/`create_public_booking`; verificado por SQL que a +10 días el canal web da 0 huecos y el manual sigue viendo todos.
- **Clientes manuales**: botón en `Clientes.tsx` reutilizando `import_customer` (cero código nuevo de backend).
- **Google Calendar** (migraciones 0021-0022): credenciales OAuth **propias por negocio** (Client ID/Secret en `business_integrations`, mismo patrón que Resend/WhatsApp) — decisión explícita del usuario tras preguntarle, no un Client ID único de plataforma. `professional_google_accounts` guarda tokens por profesional. Edge Functions: `google-oauth-start` (autenticado, firma un `state` HMAC para que nadie inicie el flujo por un profesional ajeno), `google-oauth-callback` (público, intercambia code→tokens, redirige al panel), `sync-google-event` (exporta reservas, invocada fire-and-forget desde `create-booking` y desde Agenda), `sync-google-busy` (cron cada 15 min, importa huecos ocupados como `blocks` con `source='google_calendar'`). **Descubrimiento útil**: esta sesión SÍ tiene acceso a `pg_cron`/`pg_net` vía SQL directo (contradice una nota de una fase anterior que decía que hacía falta el dashboard de Supabase) — lo único que de verdad requiere el dashboard de Supabase es configurar *secretos de Edge Functions* (no hay tool MCP para eso). Quedan 3 secretos por configurar manualmente (`GOOGLE_STATE_SECRET`, `DASHBOARD_URL`, `GOOGLE_SYNC_CRON_SECRET`, este último con el valor exacto ya embebido en el cron job — ver CLAUDE.md, no se commitea a git). Sin esos secretos el código está desplegado pero inerte (no rompe nada, simplemente no hace nada).
- Otro hallazgo de esta sesión: `apps/dashboard/wrangler.jsonc` tenía `name: "turnigo-dashboard"` pero el worker real desplegado se llama `turnigo-panel` — corregido para que un futuro `wrangler deploy` no cree un worker duplicado por error.
- Todo verificado en vivo contra Cloudflare con datos de prueba creados y luego borrados (bookings, customers) — nunca se dejó basura en el negocio demo.

**Fase 6 — ronda de ajustes tras la Fase 5, misma sesión larga (COMPLETADA y verificada en Cloudflare, 2026-09-18/19)**: el usuario pidió más cambios sobre lo recién construido, y luego una tercera tanda de ajustes finos. Todo verificado en vivo contra `https://turnigo-panel.lodesiko12.workers.dev/`, nunca localhost (instrucción explícita del usuario, ahora en `CLAUDE.md`).

*Ronda A:*
- **Usuarios múltiples por negocio (super-admin)**: nueva pestaña "Usuarios" en `BusinessDetail.tsx` + Edge Function `admin-business-users` (list/add/remove/update_role), mismo patrón de alta/vinculación de `admin-create-business`. Permite que varios profesionales tengan cuenta de acceso al mismo negocio. **No se pudo verificar en vivo** (requiere sesión de super-admin real, sin contraseña conocida) — pendiente de que el usuario lo compruebe él mismo.
- **Selector de profesional en "Nueva reserva" (manual)**: antes el staff no podía elegir profesional al crear una reserva a mano (siempre "cualquiera"); ahora aparece el mismo selector que en el widget cuando el servicio tiene >1 profesional. Verificado en vivo con reserva de prueba (creada y borrada).
- **Vista "Mes" en Agenda**: rejilla propia (sin librería, mismo estilo que la semanal ya existente), con `addMonthsYmd`/`fmtMonthYear` nuevos en el propio `Agenda.tsx` (no en `@reservas/shared`). Click en día vacío → cambia a vista Día; click en una cita → abre el modal de siempre (con `stopPropagation`). Verificado en vivo: navegación mes a mes, ambos tipos de click.
- **Reportes no mostraba reservas manuales futuras — diagnóstico real, no era un bug de channel/status**: la query de Reportes solo cubre `[hoy-N días, hoy]` (histórico), así que cualquier reserva con `starts_at` futuro (típico de una reserva manual para mañana) quedaba fuera sin más explicación. Solución: sección nueva "Próximas reservas" separada de las estadísticas históricas, sin alterar su significado. Verificado en vivo.
- **`WindowsEditor` "Rellenar rápido"**: ahora admite una segunda franja opcional (checkbox "+ Añadir segunda franja", pensado para negocios que cierran a mediodía).

*Ronda B (ajustes finos):*
- **Estados de cita simplificados**: para negocios tipo `citas`, el modal de reserva en Agenda solo ofrece marcar **completada / cancelada / ausente** (antes también mostraba confirmada/en mesa, que son conceptos de restaurante). Restaurante conserva el ciclo completo. `no_show` pasa a mostrarse como "Ausente" en toda la UI (antes "No-show"), cambio en `STATUS_TEXT` de `components/ui.tsx` (compartido, así que también afecta a restaurante — se consideró una mejora de claridad válida para ambos).
- **Borrar clientes**: icono 🗑 por fila en `Clientes.tsx` + botón "Eliminar cliente" en la ficha de detalle. `bookings.customer_id` tiene `on delete set null`, así que el historial de reservas se conserva aunque se borre la ficha del cliente.
- **Etiquetas de clientes eliminadas** de toda la UI (VIP/Habitual/Alérgico/Prensa/Problemático) — el usuario las quería fuera. La columna `customers.tags` en BD **no se tocó** (queda sin usar, no se hizo migración para dropearla; decisión deliberada de no hacer un cambio de esquema destructivo para esto).

**OJO — el negocio demo original ya no existe**: en algún punto de la sesión, `barberia-demo` ("Barbería El Corte") se borró definitivamente (probablemente el usuario probando la función de borrado de negocios de la Fase 5). Detectado porque un login con `staff@barberia.test` dejó de encontrar negocio. **A partir de ahí, todas las verificaciones en vivo de esta sesión se hicieron contra un negocio REAL del usuario**: *Ana Sánchez Psicóloga* (`slug: ana-sanchez-psicologa`). El usuario dio credenciales de acceso reales para ese negocio directamente en el chat con el único propósito de verificar en vivo — **no se guardan en este archivo versionado en git** por ser un negocio real de un cliente; si hace falta volver a entrar, pedírselas de nuevo al usuario. Cualquier dato de prueba creado ahí (reservas, clientes) se borró inmediatamente después de verificar — mantener esa disciplina si se sigue usando ese negocio para pruebas. Si se necesita un negocio de prueba desechable sin este cuidado, ofrecer recrear `barberia-demo` con `seed.sql` (el usuario no ha pedido esto todavía, quedó como opción declinada por ahora).

**Pendiente**: la lista completa y canónica de pendientes vive en el README, sección **"## Pendientes"** (justo después de "Estado y roadmap") — consultarla ahí primero en cualquier conversación futura, no reconstruirla de memoria. Resumen actualizado: verificar en vivo la pestaña "Usuarios" de super-admin (pendiente de que el usuario lo haga, ver Fase 6); Google Calendar operativo (bloqueado por 3 secretos de Edge Functions que solo el usuario puede configurar, ver `CLAUDE.md`); Stripe huella bancaria/prepago (bloqueado hasta que el usuario tenga cuenta, aunque sea de test); resumen de reseñas con IA (bloqueado hasta que el usuario tenga acceso a la API de Google Business Profile); API pública/webhooks (declinado por ahora, no bloqueado); editor visual de posiciones de mesa (drag&drop sobre croquis libre); reasignación manual de mesa para reservas con combinación (bloqueado a propósito); drag&drop en la rejilla semanal/mensual de Agenda; programar cron pg_cron para `whatsapp-reminders` y `request-reviews` (ya no bloqueado técnicamente, ver Fase 5). Repo con git en GitHub (`lodesiko12/Reservas-online`), push directo a `main` sin ramas/PRs. **Despliegue real: Cloudflare Workers** (`turnigo-panel` y `turnigo-widget`, no Netlify — ver `CLAUDE.md`, que es ahora la fuente de verdad para el flujo de verificación/despliegue).
